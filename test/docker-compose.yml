version: '2'

services:
  backend:
    image: liutsing/ws:0.0.3
    ports:
      - '9010:8010'
  frontend:
    image: liutsing/admin:0.0.4
    ports:
      - '9011:80'
networks:
  default:
    external:
      name: maple-network
